//Assignment: =
var age = 26;
var firstName = "Eirik";
var money = 50.05;
//Arithmetic: + - * / %
age = age + 1
var newAge = age + 1
age += 1;
var fullName = firstName + " Thue";
// Comparison: == != > >= < <=
var canAfford = money >= 20;
// Logical: || && !
var cannotAfford = !canAfford;
var canBuy = canAfford && age >= 18;