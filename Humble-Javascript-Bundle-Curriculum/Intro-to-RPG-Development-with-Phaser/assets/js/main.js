var config ={
    type: Phaser.AUTO,
    width: 800,
    heigth: 600,
    scene: {
        preload: preload,
        create: create,
    },
};

var game = new Phaser.Game(config);

function preload(){

}

function create(){

}